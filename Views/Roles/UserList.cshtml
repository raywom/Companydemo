@model IEnumerable<CompanyDemo.Models.User>

<h2>List Of users</h2>
<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-userid="@user.Id">Perms</a>
            </td>
        </tr>
    }
</table>